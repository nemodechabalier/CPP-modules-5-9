#pragma once

#include <string>
#include <iostream>

class Defaults
{
private:
	/* data */
public:
	Defaults();
	Defaults(const Defaults& other);
	~Defaults();
	Defaults& operator=(const Defaults& other);
};
